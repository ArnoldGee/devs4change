import MaxWidth from "./atoms/MaxWidth";

const Header = (): JSX.Element => (
  <header className="fixed inset-x-0 p-4 bg-green text-white">
    <MaxWidth>Hello, I am Ainhoa</MaxWidth>
  </header>
);

export default Header;
